# Core
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
yfinance>=0.2.40
httpx>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.6.0

# Provider SDKs — you only need the one your PROVIDER uses.
# anthropic is the default; openai also covers Ollama (local).
anthropic>=0.49.0
openai>=1.50.0
